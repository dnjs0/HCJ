<!DOCTYPE html>
<html lang="ko">
<head>
	<meta charset="UTF-8">
	<title>ex03.html</title>
</head>
<body>
	<!-- 
	
	   HTML 구성요소
	   
	   1. 태그(Tag) / 요소(Element)
	   2. 특성(Attribute)
	   3. 텍스트(PCDATA)
	   -----------------------------------------
	   4. 주석(Comment)
	   -----------------------------------------
	   5. 선언문(Declaration)
	   6. 엔티티(Entity)
	   7. 외부요소
	   8. 등등..
	
	
	<!-- 1. 태그(tag) / 요소(element)
	- 명령여를 >로 묶에 놓은 요소
	- HTML의 주요 요소
	- HTML 문서의 골격(뼈대)을 만드는 역할(레이아웃 발생)
	- 데이터를 화면에 출력하는 역할
	- 각 태그마다 역할 부여 > 출력되는 형태 or 기능 or 의미가 다르다.
	
	
	 -->
	
	
	 -->
</body>
</html>